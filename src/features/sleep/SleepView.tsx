import { Placeholder } from '../../components/Placeholder';

export function SleepView() {
  return (
    <Placeholder
      title="Sommeil"
      phase="Phase 1 — backend prêt"
      desc="Saisie des nuits, dette de sommeil et courbe d'énergie circadienne (modèle RISE). L'API /api/sleep est déjà en ligne ; l'écran de saisie arrive ici."
    />
  );
}
